{
  "name": "openx",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test": "node --test",
    "check": "find apps packages tests -name '*.js' -print0 | xargs -0 -n1 node --check && npm test"
  },
  "engines": { "node": ">=20" }
}
